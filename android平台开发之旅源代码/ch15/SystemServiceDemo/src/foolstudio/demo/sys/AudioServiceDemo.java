package foolstudio.demo.sys;

import android.media.AudioManager;

public class AudioServiceDemo {

	public static String getInfo(AudioManager service) {
		// TODO Auto-generated method stub
		StringBuffer sb = new StringBuffer();
		/*
		service.adjustStreamVolume(streamType, direction, flags);
		service.adjustSuggestedStreamVolume(direction, suggestedStreamType, flags);
		service.adjustVolume(direction, flags);
		service.getMode();
		service.getRingerMode();
		service.getRouting(mode);
		service.getStreamMaxVolume(streamType);
		service.getStreamVolume(streamType);
		service.getVibrateSetting(vibrateType);
		*/
		
		return (sb.toString() );
	}

}
